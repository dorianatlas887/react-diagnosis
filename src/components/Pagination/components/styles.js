const styles = theme => ({
  pagination: {
    display: 'flex',
    alignItems: 'center',
  },
  page: {
    marginLeft: theme.spacing.unit,
    marginRight: theme.spacing.unit,
  },
  arrow: {
    cursor: 'pointer',
  },
});

export default styles;
